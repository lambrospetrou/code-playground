CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    UNIQUE(name)
);

selectByName:
SELECT * FROM users
WHERE name = ?;

addUser:
INSERT INTO users (name)
VALUES (?);
